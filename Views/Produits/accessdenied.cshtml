@model PanierMVC.Models.Produit

@{
    ViewData["Title"] = "AccesDenied";
}

<h1>Details</h1>

<div class="row">
    <div class="col-md-4">
        <img src="@Url.Content(Model.image)" class="card-img-top product-image" alt="Image">
    </div>
    <div class="col-md-8">
        <h4>Produit</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.nom)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.nom)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.type)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.type)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.prix)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.prix) dh
            </dd>
        </dl>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

